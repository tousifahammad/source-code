1. rv_menu_items_list.smoothScrollToPosition(list.size - 1)

2. rootView.rv_menu_items_list.scrollToPosition(list.size - 1)

3. 


(rv_menu_category_center.layoutManager as LinearLayoutManager).scrollToPositionWithOffset(it, 0)